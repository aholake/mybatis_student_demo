<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tanloc.demo.service.StudentService">
	<resultMap type="student" id="result">
		<result property="id" column="id" />
		<result property="firstName" column="firstName" />
		<result property="lastName" column="lastName" />
		<result property="dateOfBirth" column="dateOfBirth" />
		<result property="score" column="score" />
	</resultMap>

	<select id="getAllStudent" resultMap="result">
		SELECT * FROM student;
	</select>

	<insert id="insertStudent" useGeneratedKeys="true" keyProperty="id"
		keyColumn="id" parameterType="student">
		INSERT INTO student (firstName, lastName, dateOfBirth, score) VALUES
		(#{firstName}, #{lastName}, #{dateOfBirth}, #{score})
		<selectKey resultType="int" keyProperty="id" order="AFTER">
			SELECT LAST_INSERT_ID();
		</selectKey>
	</insert>

	<update id="updateStudent" parameterType="student">
		UPDATE student
		SET
		firstName = #{firstName},
		lastName = #{lastName},
		dateOfBirth =
		#{dateOfBirth},
		score = #{score}
		WHERE
		id = #{id};
	</update>

	<delete id="deleteStudent" parameterType="int">
		DELETE FROM student
		WHERE id = #{id};
	</delete>

	<select id="getStudent" parameterType="int" resultType="student">
		SELECT *
		FROM student WHERE id = #{id};
	</select>
</mapper>